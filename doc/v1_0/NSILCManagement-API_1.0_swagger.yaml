swagger: '2.0'
info:
  version: "1.0"
  title: Network Slice Instance (NSI) LifeCycle Management (LCM) interface
  description: >
    Network Slice Instance (NSI) Lifecycle Management interface: This interface is aligned with the ETSI NFV IFA0013/SOL005 specification, making the required changes to adapt it to the lifecycle management of NSIs.
  license:
    name: H2020 5GTANGO copyright
    #url: '-'
  contact:
    name: H2020 5GTANGO
basePath: /api/nsilcm/v1
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /nsi:
    post:
      description: |
        **Create NSI Identifier**: Creates a NSI ID, creating a NSI Record (NSIR) in NOT_INSTANTIATED state. At this point no NSI instantiation have occurred. This step must be done before Instantiate NSI.
      parameters:
      - name: userDefinedData
        in: body
        required: true
        schema:
          description: User Info associated to the NST.
          type: object
          properties:
            nstId:
              description: Reference to the NST.
              type: integer
              format: string
            name:
              description: Humam-readable NSI name.
              type: string
              format: string
            description:
              description: Humam-readable NSI description.
              type: string
              format: string
          example:
            name: Example_NSI
            description_nsi: Decription of the NSI_Example.
            nstId: e3476dd9-259c-42ae-816f-e891ddb4e8db
      responses:
        '201':
          description: >
            Unique identifier of the nstInfoId.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
            Location:
              description: The resource URI of the created NSI.
              type: string
              format: url
          schema:
            description: >
              A json of the created and instantiated NSi.
            example:
              created_at: '2018-07-16T14:03:02.204+00:00'
              description: Decription of the NSI_Example.
              flavorId: ''
              instantiateTime: '2018-07-16T14:01:31.447547'
              name: Example_NSI
              netServInstance_Uuid:
                - e1547f09-e954-4299-bd62-138045566872
              nsiState: INSTANTIATED
              nstId: e3476dd9-259c-42ae-816f-e891ddb4e8db
              nstName: Example_NST
              nstVersion: '1.0'
              sapInfo: ''
              scaleTime: ''
              terminateTime: ''
              updateTime: ''
              updated_at: '2018-07-16T14:03:02.204+00:00'
              uuid: a75d1555-cc2c-4b96-864f-fa1ffe5c909a
              vendor: eu.5gTango
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response.
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
    get:
      description: >
        **Query NSI**: Queries NSIs information.
      responses:
        '200':
          description: Results of the query (NSIs).
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: Challenge if the corresponding HTTP request has not provided authorization, or error details if the corresponding HTTP request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            type: array
            items:
              description: Results of the query (NsiInstance).
              type: object
              properties:
                uuid:
                  description: The unique identifier of the NSI.
                  type: integer
                  example: a75d1555-cc2c-4b96-864f-fa1ffe5c909a
                name:
                  description: The name of the NSI.
                  type: string
                  example: NSI_Example
                description:
                    description: A description of the NSI.
                    type: string
                    example: NSI_descriptor
                nstId:
                    description: The Id of the NST used to instantiate the NSI.
                    type: integer
                    example: a7633ab5-bf8e-46af-a12d-4407479df863
                flavorId:
                    description: The flavor identifier of the NSI.
                    type: string
                    example: ""
                sapInfo: 
                  description: SAP of the NSI.
                  type: string # this type is not the accurate one.
                  example: ""
                nsiState:
                    description: State of the NSI (possible values INSTANTIATED, NOT_INSTANTIATED).
                    type: string
                    enum:
                      - INSTANTIATED
                      - NOT_INSTANTIATED
                nstName:
                  description: Reference uuid to the NST the NSi is base on.
                  type: string
                  example: Example_NST
                netsVersion:
                  description: Version of the NST used.
                  type: string
                  example: "1.1"
                vendor:
                  description: Vendor of the NST this NSi is base on.
                  type: string
                  example: eu.5gTango
                netServInstance_Uuid:
                  description: uuid list of the service instantiations done from by the NSI.
                  type: array
                  items:
                    type: string
                    example: e1547f09-e954-4299-bd62-138045566872
                created_at:
                  description: Datetime when this NSI is created..
                  type: string
                  example: "2018-07-16T14:03:02.204+00:00"
                instantiateTime:
                  description: Datetime when this NSIis instantiated.
                  type: string
                  format: date-time
                  example: "2018-07-16T14:01:31.447547"
                scaleTime:
                  description: Datetime of last scale operation done with this NSI.
                  type: string
                  format: date-time
                  example: ""
                terminateTime:
                  description: Datetime when this NSI is terminated.
                  type: string
                  format: date-time
                  example: ""
                updateTime:
                  description: Datetime when this NSi was updated.
                  type: string
                  format: date-time
                  example: "2018-07-16T14:03:02.204+00:00"
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response.
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
  '/nsi/{nsiId}':
    parameters:
      - name: nsiId
        description: Unique identifier of the NSI.
        in: path
        type: string
        required: true
    get:
      description: >
        **Query NSI**: Queries NSI information (multiple NSIs).
      responses:
        '200':
          description: |
            Results of the query (NSIs).
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: Challenge if the corresponding HTTP request has not provided authorization, or error details if the corresponding HTTP request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: Results of the query (NsiInstance).
            type: object
            properties:
              uuid:
                description: The unique identifier of the NSI.
                type: integer
                example: a75d1555-cc2c-4b96-864f-fa1ffe5c909a
              name:
                description: The name of the NSI.
                type: string
                example: NSI_Example
              description:
                description: A description of the NSI.
                type: string
                example: NSI_descriptor
              nstId:
                description: The Id of the NST used to instantiate the NSI.
                type: integer
                example: a7633ab5-bf8e-46af-a12d-4407479df863
              flavorId:
                description: The flavor identifier of the NSI.
                type: string
                example: ""
              sapInfo: 
                description: SAP of the NSI.
                type: string # this type is not the accurate one.
                example: ""
              nsiState:
                description: State of the NSI (possible values INSTANTIATED, NOT_INSTANTIATED).
                type: string
                enum:
                  - INSTANTIATED
                  - NOT_INSTANTIATED
              nstName:
                description: Reference uuid to the NST the NSi is base on.
                type: string
                example: Example_NST
              netsVersion:
                description: Version of the NST used.
                type: string
                example: "1.1"
              vendor:
                description: Vendor of the NST this NSi is base on.
                type: string
                example: eu.5gTango
              netServInstance_Uuid:
                description: uuid list of the service instantiations done from by the NSI.
                type: array
                items:
                  type: string
                  example: e1547f09-e954-4299-bd62-138045566872
              created_at:
                description: Datetime when this NSI is created..
                type: string
                format: date-time
                example: "2018-07-16T14:03:02.204+00:00"
              instantiateTime:
                description: Datetime when this NSIis instantiated.
                type: string
                format: date-time
                example: "2018-07-16T14:01:31.447547"
              scaleTime:
                description: Datetime of last scale operation done with this NSI.
                type: string
                format: date-time
                example: ""
              terminateTime:
                description: Datetime when this NSI is terminated.
                type: string
                format: date-time
                example: ""
              updateTime:
                description: Datetime when this NSi was updated.
                type: string
                format: date-time
                example: "2018-07-16T14:03:02.204+00:00"
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema), the API producer
            shall respond with this response code. The "ProblemDetails"
            structure shall be provided, and should include in the "detail"
            attribute more information about the source of the problem. NOTE 2:
            This error response code is only applicable for methods that have a
            request body.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI 
    # delete:
      # description: |
        # **Delete NSI Identifier**: Deletes the NSI ID (if in NOT_INSTANTIATED state).To delete the NSI ID, first the NSI must be terminate (Terminate NSI).
      # responses:
        # '201':
          # description: >
            # Unique identifier of the nsiId.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NSI.
              # type: string
              # format: url
          # schema:
            # description: >
              # A unique identifier of the NSI.
              # type: integer
              # name: nsiId
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI                
  '/nsi/{nsiId}/terminate':
    parameters:
      - name: nsiId
        description: Identifier of the NSI.
        in: path
        type: string
        required: true
    post:
      description: |
        **Instantiate NSI**: Instantiate a NSI (in NOT_INSTANTIATED state). Before this, a NSI ID creation needs to be done (Create NSI ID).
      parameters:
        - name: InstantiateNsiRequest
          description: Data associated to a NSI instantiation.
          in: body
          required: true
          schema:
            description: Data associated to the NSI.
            type: object
            properties:
              terminateTime:
                description: Timestamp indicating when the instantiation will be tigerred (0 means immediatly).
                type: string
                format: date-time                      
      responses:
        '202':
          description: >
            A json with the information of the terminated NSI.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
            Location:
              description: The resource URI of the created NSI.
              type: string
              format: url
          schema: # consider not return anything?
            description: >
              A unique identifier of lifecycle occurrence.
            example:
              created_at: '2018-07-16T14:03:02.204+00:00'
              description: Decription of the NSI_Example.
              flavorId: ''
              instantiateTime: '2018-07-16T14:01:31.447547'
              name: Example_NSI
              netServInstance_Uuid:
                - e1547f09-e954-4299-bd62-138045566872
              nsiState: TERMINATED
              nstId: e3476dd9-259c-42ae-816f-e891ddb4e8db
              nstName: Example_NST
              nstVersion: '1.0'
              sapInfo: ''
              scaleTime: ''
              terminateTime: '2018-07-20T17:52:12.204+00:00'
              updateTime: ''
              updated_at: '2018-07-16T14:03:02.204+00:00'
              uuid: a75d1555-cc2c-4b96-864f-fa1ffe5c909a
              vendor: eu.5gTango
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type: 
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
  # '/nsi/{nsiId}/scale':
    # parameters:
      # - name: nsiId
        # description: Identifier of the NSI.
        # in: path
        # type: string
        # required: true
    # post:
      # description: |
        # **Scale NSI**: Scale a NSI, indicating the scaling details. Scaling can increase or decrease the NSI performance and therefore the resources associated to it.
      # parameters:
        # - name: ScaleNsiRequest
          # description: Data associated to a NSI scaling.
          # in: body
          # required: true
          # schema:
            # description: Data associated to the NSI.
            # type: object
            # required:
                # - scaleNsiData
                # - scaleTime
            # properties:
                # scaleNsiData:
                  # description: Necessary data to scale the NSI.
                  # type: string # maybe other type e.g. hash
                # scaleTime:
                    # description: Timestamp indicating when the scale will be tigerred (0 means immediatly).
                    # type: string
                    # format: date-time
      # responses:
        # '202':
          # description: >
            # Confirmation of the Lifecycle Operation (Scaling) Id??.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NSI.
              # type: string
              # format: url
          # schema: # consider not return anything?
            # description: >
              # A unique identifier of lifecycle occurrence.
              # type: integer
              # name: lifecycleOperationOccurrenceId
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type: 
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
  # '/nsi/{nsiId}/update':
    # parameters:
      # - name: nsiId
        # description: Identifier of the NSI.
        # in: path
        # type: string
        # required: true
    # post:
      # description: |
        # **Update NSI**: Update a NSI, changing any aspect suitable to be changing during runtime.
      # parameters:
        # - name: ScaleNsiRequest
          # description: Data associated to a NSI scaling.
          # in: body
          # required: true
          # schema:
            # description: Data associated to the NSI.
            # type: object
            # required:
                # - updateType
            # properties:
                # updateType:
                    # description: Type of NSI update (possible values INSTANTIATE_NS, REMOVE_NS, CHANGE_NS_DF, ADD_SAP, REMOVE_SAP).
                    # type: string
                    # enum:
                      # - INSTANTIATE_NS
                      # - REMOVE_NS
                      # - CHANGE_NS_DF
                      # - ADD_SAP
                      # - REMOVE_SAP
                # addNs:
                  # description: NSs to be added to the Slice.
                  # type: array
                  # items:
                    # type: string
                # removeNs:
                  # description: NSs to be removed from the Slice.
                  # type: array
                  # items:
                    # type: string
                # addSap:
                  # description: SAPs to be added to the Slice.
                  # type: array
                  # items:
                    # type: string
                # removeSap:
                  # description: SAps to be removed from the Slice.
                  # type: array
                  # items:
                    # type: string
                # updateTime:
                    # description: Timestamp indicating when the update will be tigerred (0 means immediatly).
                    # type: string
                    # format: date-time
      # responses:
        # '202':
          # description: >
            # Confirmation of the Lifecycle Operation (Updating) Id??.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NSI.
              # type: string
              # format: url
          # schema: # consider not return anything?
            # description: >
              # A unique identifier of lifecycle occurrence.
              # type: integer
              # name: lifecycleOperationOccurrenceId
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type: 
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI                
  # '/nsi/{nsiId}/heal':
    # parameters:
      # - name: nsiId
        # description: Identifier of the NSI.
        # in: path
        # type: string
        # required: true
    # post:
      # description: |
        # **Heal NSI**: Heal a NSI, indicating the healing details. Scaling can heal a NSI from a failure situation (it can be seen as a subset of the Update NSI).
      # parameters:
        # - name: HealNsiRequest
          # description: Data associated to a NSI scaling.
          # in: body
          # required: true
          # schema:
            # description: Data associated to the NSI.
            # type: object
            # required:
                # - degreeHealing
                # - actionsHealing
            # properties:
                # degreeHealing:
                  # description: Degree of healing (HEAL_RESTORE, HEAL_QOS, HEAL_RESET, PARTIAL_HEALING).
                  # type: string
                  # enum: 
                  # -  HEAL_RESTORE # Complete the healing of the NS restoring the state of the NS before the failure occurred
                  # -  HEAL_QOS # Complete the healing of the NS based on the newest QoS values
                  # -  HEAL_RESET # Complete the healing of the NS resetting to the original instantiation state of the NS
                  # -  PARTIAL_HEALING
                # actionsHealing:
                  # description: Actions to be taken for healing.
                  # type: array 
                  # items:
                    # type: string
                # additionalParamsForNsi:
                  # description: Additional parameters to consider together with the actionssHealing.
                  # type: array 
                  # items:
                    # type: string # must be a hash?
      # responses:
        # '202':
          # description: >
            # Confirmation of the Lifecycle Operation (Healing) Id??.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NSI.
              # type: string
              # format: url
          # schema: # consider not return anything?
            # description: >
              # A unique identifier of lifecycle occurrence.
              # type: integer
              # name: lifecycleOperationOccurrenceId
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type: 
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
  # '/nsi/{nsiId}/terminate':
    # parameters:
      # - name: nsiId
        # description: Identifier of the NSI.
        # in: path
        # type: string
        # required: true
    # post:
      # description: |
        # **Terminate NSI**: Terminates a NSI (if in INSTANTIATED state), releasing all the resources associated to it.
      # parameters:
        # - name: TerminateNsiRequest
          # description: Data associated to a NSI termination.
          # in: body
          # required: true
          # schema:
            # description: Data associated to the NSI.
            # type: object
            # properties:
                # terminateTime:
                    # description: Timestamp indicating when the termination will be tigerred (0 means immediatly).
                    # type: string
                    # format: date-time
      # responses:
        # '202':
          # description: >
            # Confirmation of the Lifecycle Operation (Termination) Id??.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NSI.
              # type: string
              # format: url
          # schema: # consider not return anything?
            # description: >
              # A unique identifier of lifecycle occurrence.
              # type: integer
              # name: lifecycleOperationOccurrenceId
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type: 
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
  # /subscriptions:
    # post:
      # description: |
        # **Subscribe**: Subscribes to notifications related to NSI management changes.
      # parameters:
        # - name: filter
          # description: Filter to match the NSIs.
          # in: body
          #required: true
          # schema:
            # description: Filter to match the NSIs.
            # type: object
            # required:
              # - callbackUri
            # properties:
              # filter:
                # description: >
                  # A subscription filter related to notifications related to NSI management.
                # type: object
                # properties:
                 # nstId:
                  # description: The NST Id.
                  # type: string
                 # nsiId:
                  # description: A NSI Id.
                  # type: string
                 # nsiName:
                    # description: NST Name.
                    # type: string
                 # notificationTypes:
                    # description: >
                      # Permited values: NsiLcmOperationOccurenceNotification, NsiIdentifierCreationNotification, NsiIdentifierDeletionNotification and NsiChangeNotification.
                    # type: string
                    # enum:
                        # - NsiLcmOperationOccurenceNotification
                        # - NsiIdentifierCreationNotification
                        # - NsiIdentifierDeletionNotification
                        # - NsiChangeNotification
                 # operationTypes:
                    # description: >
                      # Permited values: INSTANTIATE, SCALE, UPDATE, TERMINATE and HEAL.
                    # type: string
                    # enum:
                        # - INSTANTIATE
                        # - SCALE
                        # - UPDATE
                        # - TERMINATE
                        # - HEAL
                 # operationStates:
                    # description: >
                      # Permited values: PROCESSING, COMPLETED, PARTIALLY_COMPLETED, AILED_TEMP, FAILED, ROLLING_BACK and ROLLED_BACK.
                    # type: string
                    # enum:
                        # - PROCESSING
                        # - COMPLETED
                        # - PARTIALLY_COMPLETED
                        # - AILED_TEMP
                        # - FAILED
                        # - ROLLING_BACK
                        # - ROLLED_BACK
              # callbackUri:
                # description: |
                  # String formatted according to IETF RFC 3986.
                # type: string
              # authentication:
                # type: object
                # required:
                  # - authType
                # properties:
                  # authType:
                    # description: >
                      # Defines the type of Authentication Authorization to use.
                      # when sending a notification.  Permitted values: * BASIC:
                      # In every POST request that sends a notification, use
                        # HTTP Basic authentication with the client credentials.
                      # * OAUTH2_CLIENT_CREDENTIALS: In every POST request that
                      # sends a
                        # notification, use an OAuth 2.0 Bearer token, obtained using the
                        # client credentials grant type.
                    # type: string
                    # enum:
                      # - BASIC
                      # - OAUTH2_CLIENT_CREDENTIALS
                  # paramsBasic:
                    # description: >
                      # Parameters for authentication/authorization using BASIC.
                      # Shall be present if authType is "BASIC" and the contained
                      # information has not been provisioned out of band. Shall be
                      # absent otherwise.
                    # type: object
                    # properties:
                      # userName:
                        # description: >
                          # Username to be used in HTTP Basic authentication.
                          # Shall be present if it has not been provisioned out of
                          # band.
                        # type: string
                      # password:
                        # description: >
                          # Password to be used in HTTP Basic authentication.
                          # Shall be present if it has not been provisioned out of
                          # band.
                        # type: string
                  # paramsOauth2ClientCredentials:
                    # description: >
                      # Parameters for authentication/authorization using
                      # OAUTH2_CLIENT_CREDENTIALS. Shall be present if authType is
                      # "OAUTH2_CLIENT_CREDENTIALS" and the contained information
                      # has not been provisioned out of band. Shall be absent
                      # otherwise.
                    # type: object
                    # properties:
                      # clientId:
                        # description: >
                          # Client identifier to be used in the access token
                          # request of the OAuth 2.0 client credentials grant
                          # type.  Shall be present if it has not been provisioned
                          # out of band. The clientId and clientPassword passed in
                          # a subscription shall not be the same as the clientId
                          # and clientPassword that are used to obtain
                          # authorization for API requests. Client credentials may
                          # differ between subscriptions. The value of
                          # clientPassword should be generated by a random
                          # process.
                        # type: string
                      # clientPassword:
                        # description: >
                          # Client password to be used in the access token request
                          # of the OAuth 2.0 client credentials grant type.  Shall
                          # be present if it has not been provisioned out of band.
                          # The clientId and clientPassword passed in a
                          # subscription shall not be the same as the clientId and
                          # clientPassword that are used to obtain authorization
                          # for API requests. Client credentials may differ
                          # between subscriptions. The value of clientPassword
                          # should be generated by a random process.
                        # type: string
                      # tokenEndpoint:
                        # description: |
                          # String formatted according to IETF RFC 3986.
                        # type: string
      # responses:
        # '201':
          # description: >
            # Representation of the created subscription resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NST instance
              # type: string
              # format: url
          # schema:
            # type: array
            # items:
              # description: >
                # This type represents a subscription related to notifications about NST management.
              # type: object
              # required:
                # - id
                # - callbackUri
                # - _links
              # properties:
                # id:
                  # description: |
                    # Unique identifier of the subscription.
                  # type: string
                # callbackUri:
                  # description: |
                    # String formatted according to IETF RFC 3986.
                  # type: string
                # _links:
                  # description: |
                    # Links to resources related to this resource.
                  # type: object
                  # required:
                    # - self
                  # properties:
                    # self:
                      # description: |
                        # This type represents a link to a resource.
                      # type: object
                      # required:
                        # - href
                      # properties:
                        # href:
                          # description: |
                            # URI of the referenced resource.
                          # type: string
                          # format: url
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
    # get:
      # description: >
        # **Query Subscription Info**: Queries the subscription information.
      # responses:
        # '200':
          # description: >
            # Representation of the subscription resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NST instance.
              # type: string
              # format: url
          # schema:
            # type: array
            # items:
              # description: >
                # This type represents a subscription related to notifications  about NST management.
              # type: object
              # required:
                # - id
                # - callbackUri
                # - _links
              # properties:
                # id:
                  # description: |
                    # Unique identifier of the subscription.
                  # type: string
                # callbackUri:
                  # description: |
                    # String formatted according to IETF RFC 3986.
                  # type: string
                # _links:
                  # description: |
                    # Links to resources related to this resource.
                  # type: object
                  # required:
                    # - self
                  # properties:
                    # self:
                      # description: |
                        # This type represents a link to a resource.
                      # type: object
                      # required:
                        # - href
                      # properties:
                        # href:
                          # description: |
                            # URI of the referenced resource.
                          # type: string
                          # format: url
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
  # '/subscriptions/{subscriptionId}':
    # parameters:
      # - name: subscriptionId
        # description: >
          # Unique identifier of this subscription.
        # in: path
        # type: string
        # required: true
    # get:
      # description: |
        # **Query Subscription Info**: Queries the subscription info related to NSI management changes.
      # responses:
        # '200':
          # description: |
            # Representation of the subscription resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # This type represents a subscription related to notifications about NST management.
            # type: object
            # required:
              # - id
              # - callbackUri
              # - _links
            # properties:
              # id:
                # description: |
                  # A unique identifier of the subscription.
                # type: string
              # filter:
                # description: >
                  # This represents a subscription filter related to
                  # notifications related to NST management. 
                # type: object
                # properties:
                 # id:
                  # description: The unique identifier of the NSI.
                  # type: integer
                 # nsiName:
                  # description: The name of the NSI.
                  # type: string
                 # nsiDescription:
                    # description: A description of the NSI.
                    # type: string
                 # nstId:
                    # description: The Id of the NST used to instantiate the NSI.
                    # type: integer
                 # nstInfoId:
                    # description: The Id of the NST Info of the NST used to instantiate the NSI.
                    # type: string
                 # flavorId:
                    # description: The flavor identifier of the NSI.
                    # type: string
                 # sapInfo: 
                  # description: SAP of the NSI.
                  # type: string # this type is not the accurate one.
                 # nsiState:
                    # description: State of the NSI (possible values INSTANTIATED, NOT_INSTANTIATED).
                    # type: string
                    # enum:
                      # - INSTANTIATED
                      # - NOT_INSTANTIATED
              # callbackUri:
                # description: |
                  # String formatted according to IETF RFC 3986.
                # type: string
              # _links:
                # description: |
                  # Links to resources related to this resource.
                # type: object
                # required:
                  # - self
                # properties:
                  # self:
                    # description: |
                      # This type represents a link to a resource.
                    # type: object
                    # required:
                      # - href
                    # properties:
                      # href:
                        # description: |
                          # URI of the referenced resource.
                        # type: string
                        # format: url
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
    # delete:
      # description: |
        # Terminate Subscription: Terminate an existing subscription.
      # responses:
        # '204':
          # description: |
            # The subscription resource was deleted successfully.
          # headers:
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
  # /nsi_lcm_op_occs:
    # get:
      # description: >
        # Query the status information about NSI lifecycle management operations.
      # responses:
        # '200':
          # description: >
            # Status information for NSI lifecycle management operation occurrences.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # NSI lifecycle management operation.
              # occurrence.
            # type: object
            # required:
                # - id
                # - nsiName
                # - nsiDescription
                # - nstId
                # - nstInfoId
                # - flavorId
                # - sapInfo
                # - nsiState
            # properties:
                # id:
                  # description: The unique identifier of the NSI.
                  # type: integer
                # nsiName:
                  # description: The name of the NSI.
                  # type: string
                # nsiDescription:
                    # description: A description of the NSI.
                    # type: string
                # nstId:
                    # description: The Id of the NST used to instantiate the NSI.
                    # type: integer
                # nstInfoId:
                    # description: The Id of the NST Info of the NST used to instantiate the NSI.
                    # type: string
                # flavorId:
                    # description: The flavor identifier of the NSI.
                    # type: string
                # sapInfo: 
                  # description: SAP of the NSI.
                  # type: string # this type is not the accurate one.
                # nsiState:
                    # description: State of the NSI (possible values INSTANTIATED, NOT_INSTANTIATED).
                    # type: string
                    # enum:
                      # - INSTANTIATED
                      # - NOT_INSTANTIATED
        # '400':
          # description: >
            # It fhe request is malformed or syntactically incorrect (e.g. if the
            # request URI contains incorrect query parameters or a syntactically
            # incorrect payload body), the API producer shall respond with this
            # response code. The "ProblemDetails" structure shall be provided, and
            # should include in the "detail" attribute more information about the
            # source of the problem. If the request contains a malformed access
            # token, the API producer should respond with this response. The
            # details of the error shall be returned in the WWW-Authenticate HTTP
            # header, as defined in IETF RFC 6750 [11] and IETF RFC 7235 [16]. The
            # ProblemDetails structure may be provided. If there is an application
            # error related to the client's input that cannot be easily mapped to
            # any other HTTP response code ("catch all error"), the API producer
            # shall respond with this response code.The "ProblemDetails" structure
            # shall be provided, and shall include in the "detail" attribute more
            # information about the source of the problem.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required,
            # or if the request contains an authorization token that is invalid
            # (e.g. expired or revoked), the API producer should respond with this
            # response. The details of the error shall be returned in the
            # WWW-Authenticate HTTP header, as defined in IETF RFC 6750 [11] and
            # IETF RFC 7235 [16]. The ProblemDetails structure may be provided.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request
            # to a particular resource, the API producer shall respond with this
            # response code. The "ProblemDetails" structure shall be provided.  It
            # should include in the "detail" attribute information about the
            # source of the problem, and may indicate how to solve it.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the
            # resource addressed by the URI passed in the request, or is not
            # willing to disclose that one exists, it shall respond with this
            # response code.  The "ProblemDetails" structure may be provided,
            # including in the "detail" attribute information about the source of
            # the problem, e.g. a wrong resource URI variable.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the
            # resource addressed by the URI passed in the request, or is not
            # willing to disclose that one exists, it shall respond with this
            # response code.  The "ProblemDetails" structure may be provided,
            # including in the "detail" attribute information about the source of
            # the problem, e.g. a wrong resource URI variable.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a
            # content type that is acceptable to the API producer, the API
            # producer shall respond with this response code. The "ProblemDetails"
            # structure may be omitted in that case.        
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfilment of the
            # current request, or the current resource state is inconsistent with
            # the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input
            # that cannot be easily mapped to any other HTTP response code ("catch
            # all error"), the API producer shall respond withthis response code.
            # The "ProblemDetails" structure shall be provided, and shall include
            # in the "detail" attribute more information about the source of the
            # problem.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of
            # itself or of a system it relies on, it should respond with this
            # response code, following the provisions in IETF RFC 7231 [13] for
            # the use of the "Retry-After" HTTP header and for the alternative to
            # refuse the connection. The "ProblemDetails" structure may be
            # omitted.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
  # '/nsi_lcm_op_occs/{nsiLcmOpOccId}':
    # parameters:
      # - name: nsiLcmOpOccId
        # description: >
          # Identifier of a NSI lifecycle management operation occurrence.
        # in: path
        # required: true
        # type: string
    # get:
      # description: >
        # Query the status information about NSI lifecycle management operations.
      # responses:
        # '200':
          # description: >
            # Status information for NSI lifecycle management operation occurrences.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # NSI lifecycle management operation.
            # type: object
            # required:
                # - id
                # - nsiName
                # - nsiDescription
                # - nstId
                # - nstInfoId
                # - flavorId
                # - sapInfo
                # - nsiState
            # properties:
                # id:
                  # description: The unique identifier of the NSI.
                  # type: integer
                # nsiName:
                  # description: The name of the NSI.
                  # type: string
                # nsiDescription:
                    # description: A description of the NSI.
                    # type: string
                # nstId:
                    # description: The Id of the NST used to instantiate the NSI.
                    # type: integer
                # nstInfoId:
                    # description: The Id of the NST Info of the NST used to instantiate the NSI.
                    # type: string
                # flavorId:
                    # description: The flavor identifier of the NSI.
                    # type: string
                # sapInfo: 
                  # description: SAP of the NSI.
                  # type: string # this type is not the accurate one.
                # nsiState:
                    # description: State of the NSI (possible values INSTANTIATED, NOT_INSTANTIATED).
                    # type: string
                    # enum:
                      # - INSTANTIATED
                      # - NOT_INSTANTIATED                        
        # '400':
          # description: >
            # It fhe request is malformed or syntactically incorrect (e.g. if the
            # request URI contains incorrect query parameters or a syntactically
            # incorrect payload body), the API producer shall respond with this
            # response code. The "ProblemDetails" structure shall be provided, and
            # should include in the "detail" attribute more information about the
            # source of the problem. If the request contains a malformed access
            # token, the API producer should respond with this response. The
            # details of the error shall be returned in the WWW-Authenticate HTTP
            # header, as defined in IETF RFC 6750 [11] and IETF RFC 7235 [16]. The
            # ProblemDetails structure may be provided. If there is an application
            # error related to the client's input that cannot be easily mapped to
            # any other HTTP response code ("catch all error"), the API producer
            # shall respond with this response code.The "ProblemDetails" structure
            # shall be provided, and shall include in the "detail" attribute more
            # information about the source of the problem.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required,
            # or if the request contains an authorization token that is invalid
            # (e.g. expired or revoked), the API producer should respond with this
            # response. The details of the error shall be returned in the
            # WWW-Authenticate HTTP header, as defined in IETF RFC 6750 [11] and
            # IETF RFC 7235 [16]. The ProblemDetails structure may be provided.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request
            # to a particular resource, the API producer shall respond with this
            # response code. The "ProblemDetails" structure shall be provided.  It
            # should include in the "detail" attribute information about the
            # source of the problem, and may indicate how to solve it.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the
            # resource addressed by the URI passed in the request, or is not
            # willing to disclose that one exists, it shall respond with this
            # response code.  The "ProblemDetails" structure may be provided,
            # including in the "detail" attribute information about the source of
            # the problem, e.g. a wrong resource URI variable.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the
            # resource addressed by the URI passed in the request, or is not
            # willing to disclose that one exists, it shall respond with this
            # response code.  The "ProblemDetails" structure may be provided,
            # including in the "detail" attribute information about the source of
            # the problem, e.g. a wrong resource URI variable.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a
            # content type that is acceptable to the API producer, the API
            # producer shall respond with this response code. The "ProblemDetails"
            # structure may be omitted in that case.        
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfilment of the
            # current request, or the current resource state is inconsistent with
            # the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input
            # that cannot be easily mapped to any other HTTP response code ("catch
            # all error"), the API producer shall respond withthis response code.
            # The "ProblemDetails" structure shall be provided, and shall include
            # in the "detail" attribute more information about the source of the
            # problem.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of
            # itself or of a system it relies on, it should respond with this
            # response code, following the provisions in IETF RFC 7231 [13] for
            # the use of the "Retry-After" HTTP header and for the alternative to
            # refuse the connection. The "ProblemDetails" structure may be
            # omitted.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI