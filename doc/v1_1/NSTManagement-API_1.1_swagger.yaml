swagger: '2.0'
info:
  version: "1.1"
  title: Network Slice Template (NST) Management interface
  description: >
    Network Slice Template (NST) interface: This interface is aligned with the ETSI NFV IFA0013/SOL005 specification, making the required changes to adapt it to the management of NSTs.
  license:
    name: H2020 5GTANGO copyright
    #url: '-'
  contact:
    name: H2020 5GTANGO
basePath: /api/nst/v1
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /descriptors:
    post:
      description: |
        **Create NST Info**: Create a NetSlice Template Info object and the whole NST item information presented as JSON. After NST creation, the Upload NST completes the on-boarding operation. 
      parameters:
        - name: userDefinedData
          description: Data associated to a NST.
          in: body
          required: true
          schema:
            description: User Info associated to the NST.
            type: object
            #required: # Need to check what NST Info values make sense
            #  - id
            properties:
              name:
                description: Name of the NetSLice Template
                type: string
                format: string
              version:
                description: Version of the NetSLice Template
                type: string
                format: string
              author:
                description: Author name of the NetSLice Template
                type: string
                format: string
              vendor:
                description: Vendor name of the NetSLice Template
                type: string
                format: string
              description:
                description: Description of the NetSLice Template
                type: string
                format: string
              sliceServices:
                type: array
                items:
                  type: object
                  properties:
                    servname:
                      type: string
                      description: Uuid of the Network Service to be in the Template
                    nsdID:
                      type: string
                      description: Uuid of the Network Service to be in the Template
                    slaID:
                      type: string
                      description: Uuid of the Network Service to be in the Template
            example:
              name: Example_NST
              version: 1.1
              author: 5gTango
              vendor: 5gTango
              description: This is a description of a NS
              sliceServices:
                - servname: service_name
                  nsdID: 66a1c857-76d7-48db-98a1-6674b531b010
                  slaID: a365dd7e-42c7-4db1-947f-1e2de4e432cc
                - servname: service_name
                  nsdID: 66a1c857-76d7-48db-98a1-6674b531b010
                  slaID: a365dd7e-42c7-4db1-947f-1e2de4e432cc            
      responses:
        '201':
          description: Netslice Template structure.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
            Location:
              description: The resource URI of the created NSI instance.
              type: string
              format: url
          schema:
            description: The generated NST object.
            example:
              created_at: '2018-07-19T12:40:41.419+00:00'
              md5: 42510e3a015bd1468b85987763566376
              nstd:
                name: Example_NST
                version: 1.1
                author: 5gTango NetSlicer
                vendor: 5gTango
                sliceServices: 
                  - nsdRef: 6a1c857-76d7-48db-98a1-6674b531b010
                    nsdName: service_name
                    slaID: a365dd7e-42c7-4db1-947f-1e2de4e432cc
                    is-shared: False
                5qi_value: 1
                notificationTypes: ''
                onboardingState: ENABLED
                operationalState: ENABLED
                NSI_list_ref: [6a1c857-76d7-48db-98a1-6674b531b010, a365dd7e-42c7-4db1-947f-1e2de4e432cc]
                usageState: IN_USE
              signature: null
              status: active
              updated_at: '2018-07-19T12:43:06.691+00:00'
              username: null
              uuid: 66a1c857-76d7-48db-98a1-6674b531b010
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response.
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
    get:
      description: >
        **Query NST**: Queries NST information (multiple NSTs).
      responses:
        '200':
          description: |
            Results of the query (NSTs).
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: Challenge if the corresponding HTTP request has not provided authorization, or error details if the corresponding HTTP request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            type: array
            items:
              description: Results of the query (NST).
              type: object
              properties:
                uuid:
                  description: >
                      A unique identifier of the NST allocated by the Slice Manager.
                  type: string
                created_at:
                  description: >
                      Describe the datetime when it was created.
                  type: string
                updated_at:
                  description: >
                      Describe the datetime when it was updated.
                  type: string
                name:
                    description: NST Name.
                    type: string
                version:
                    description: NST Version.
                    type: string
                author:
                    description: NST author.
                    type: string
                vendor:
                    description: NST vendor.
                    type: string
                nstNsdIds:
                  description: List of underlying NSDs that comprise the NST.
                  type: string
                onboardingState:
                  description: Operational state (ENABLE, DISABLED).
                  type: string
                  enum:
                    - ENABLED
                    - DISABLED
                operationalState:
                  description: Operational state (ENABLE, DISABLED).
                  type: string
                  enum:
                    - ENABLED
                    - DISABLED
                usageState:
                  description: Usage state (IN_USE, NOT_IN_USE).
                  type: string
                  enum:
                    - IN_USE
                    - NOT_IN_USE
                notificationTypes:
                  description: >
                    Permited values: NstOnboardingNotification, NStTChangeNotification and NstDeletionNotification.
                  type: string
                  enum:
                    - NstOnboardingNotification
                    - NstChangeNotification
                    - NstDeletionNotification
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response.
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
  '/descriptors/{nstId}':
    parameters:
      - name: nstId
        description: >
          Uuid of the on-boarded NST.
        in: path
        type: string
        required: true
    get:
      description: |
        **Query NST**: Get a particular NST information.
      responses:
        '200':
          description: |
            NST Information.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided authorization, or error details if the corresponding HTTP request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: The generated NST object.
            example:
              created_at: '2018-07-19T12:40:41.419+00:00'
              md5: 42510e3a015bd1468b85987763566376
              nstd:
                name: Example_NST
                version: 1.1
                author: 5gTango NetSlicer
                vendor: 5gTango
                sliceServices: 
                  - nsdRef: 6a1c857-76d7-48db-98a1-6674b531b010
                    nsdName: service_name
                    slaID: a365dd7e-42c7-4db1-947f-1e2de4e432cc
                    is-shared: False
                5qi_value: 1
                notificationTypes: ''
                onboardingState: ENABLED
                operationalState: ENABLED
                NSI_list_ref: [6a1c857-76d7-48db-98a1-6674b531b010, a365dd7e-42c7-4db1-947f-1e2de4e432cc]
                usageState: IN_USE
              signature: null
              status: active
              updated_at: '2018-07-19T12:43:06.691+00:00'
              username: null
              uuid: 66a1c857-76d7-48db-98a1-6674b531b010
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response.
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI                
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
    delete:
      description: |
        **Delete NST**: Delete a NST. After deletion, there is no possible anymore to create Network Slice Instances (NSIs).
      responses:
        '204':
          description: |
            The NST was deleted successfully.
          headers:
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
        '400':
          description: >
            If the request is malformed or syntactically incorrect.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '401':
          description: >
            If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '403':
          description: >
            If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '404':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '405':
          description: >
            If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '406':
          description: >
            If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '409':
          description: >
            Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '412':
          description: >
            This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '416':
          description: >
            This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '422':
          description: >
            If the payload body of a request contains syntactically correct data
            (e.g. well-formed JSON) but the data cannot be processed (e.g.
            because it fails validation against a schema).
          headers:
            Content-Type:
              description: The MIME type of the body of the response.
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              The definition of the general "ProblemDetails" data structure from
              IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              general framework defined in IETF RFC 7807 [19], the "status" and
              "detail" attributes are mandated to be included by the present
              document, to ensure that the response contains additional textual
              information about an error. IETF RFC 7807 [19] foresees
              extensibility of the "ProblemDetails" type. It is possible that
              particular APIs in the present document, or particular
              implementations, define extensions to define additional attributes
              that provide more information about the error. The description
              column only provides some explanation of the meaning to Facilitate
              understanding of the design. For a full description, see IETF RFC
              7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  A URI reference according to IETF RFC 3986 [5] that identifies
                  the problem type. It is encouraged that the URI provides
                  human-readable documentation for the problem (e.g. using HTML)
                  when dereferenced. When this member is not present, its value
                  is assumed to be "about:blank".
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type. It should
                  not change from occurrence to occurrence of the problem,
                  except for purposes of localization. If type is given and
                  other than "about:blank", this attribute shall also be
                  provided. A short, human-readable summary of the problem
                  type.  It SHOULD NOT change from occurrence to occurrence of
                  the problem, except for purposes of localization (e.g., using
                  proactive content negotiation; see [RFC7231], Section 3.4).
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem. The
                  HTTP status code ([RFC7231], Section 6) generated by the
                  origin server for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A human-readable explanation specific to this occurrence of
                  the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem. It may yield further information if dereferenced.
                type: string
                format: URI                
        '500':
          description: >
            If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
        '503':
          description: >
            If the API producer encounters an internal overload situation of itself or of a system it relies on.
          headers:
            Content-Type:
              description: The MIME type of the body of the response ("application/problem+json").
              type: string
              maximum: 1
              minimum: 1
            WWW-Authenticate:
              description: >
                Challenge if the corresponding HTTP request has not provided
                authorization, or error details if the corresponding HTTP
                request has provided an invalid authorization token.
              type: string
              maximum: 1
              minimum: 0
          schema:
            description: >
              General "ProblemDetails" data structure from IETF RFC 7807 [19].
            type: object
            required:
              - status
              - detail
            properties:
              type:
                description: >
                  Problem type according to IETF RFC 3986 [5]
                type: string
                format: URI
              title:
                description: >
                  A short, human-readable summary of the problem type.
                type: string
              status:
                description: >
                  The HTTP status code for this occurrence of the problem.
                type: integer
              detail:
                description: >
                  A longer explanation of the specific occurrence of the problem.
                type: string
              instance:
                description: >
                  A URI reference that identifies the specific occurrence of the
                  problem.
                type: string
                format: URI
    # patch:
      # description: |
        # **Update NST Info**: Update a NST. After on-boarding, the NSI info can be changed.
      # parameters:
        # - name: userDefinedData
          # description: Data associated to a NST, but included in the descriptor (in a key-value format).
          # in: body
          # required: true
          # schema:
            # description: Filter to match the NSTs.
            # type: object
            # required:
              # - id
            # properties:
              # id:
                  # description: >
                      # A unique identifier of the NST allocated by the Slice Manager.
                  # type: string
              # nstId:
                  # description: A unique identifier assigned by the NST designer.
                  # type: string
              # nstName:
                    # description: NST Name.
                    # type: string
              # nstVersion:
                    # description: NST Version.
                    # type: string
              # nstDesigner:
                    # description: NST Designer.
                    # type: string
              # nstInvariantId:
                    # description: >
                      # NST Identifier of a NST (is the same for different versions of a NST).
                    # type: string
              # nstNsdIds:
                  # description: List of underlying NSDs that comprise the NST.
                  # type: string
              # nstOnboardingState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
              # nstOperationalState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
              # nstUsageState:
                    # description: Usage state (IN_USE, NOT_IN_USE).
                    # type: string
                    # enum:
                      # - IN_USE
                      # - NOT_IN_USE
              # notificationTypes:
                    # description: >
                      # Permited values: NstOnboardingNotification, NStTChangeNotification and NstDeletionNotification.
                    # type: string
                    # enum:
                        # - NstOnboardingNotification
                        # - NstChangeNotification
                        # - NstDeletionNotification
              # userDefinedData: #this type is not the most accurate (hash?)
                  # description: NST user defined data.
                  # type: string
        # - name: nstOperationalState
          # description: >
             # Operational state (ENABLE, DISABLED).
          # type: string
          # required: true
          # in: path
          # enum:
            # - ENABLED
            # - DISABLED
      # responses:
        # '204':
          # description: |
            # The NST was updated successfully.
          # headers:
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI                
  # '/descriptors/{nstInfoId}/nst_content':
    # parameters:
      # - name: nstInfoId
        # description: >
          # Identifier of the on-boarded NST.
        # in: path
        # type: string
        # required: true
    # get:
      # description: |
        # **Fetch NST**: Fetch a particular NST (the file? or the fields?).
      # responses:
        # '200':
          # description: |
            # NST Information.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided authorization, or error details if the corresponding HTTP request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
              # description: Results of the query (NST).
              # type: object
              # required:
                # - id
                # - nstId
                # - nstName
                # - nstVersion
                # - nstDesigner
                # - nstInvariantId
                # - nstNsdIds
                # - nstOnboardingState
                # - nstOperationalState
                # - nstUsageState
                # - notificationTypes
                # - userDefinedData
              # properties:
                # id:
                  # description: >
                      # A unique identifier of the NST allocated by the Slice Manager.
                  # type: string
                # nstId:
                  # description: A unique identifier assigned by the NST designer.
                  # type: string
                # nstName:
                    # description: NST Name.
                    # type: string
                # nstVersion:
                    # description: NST Version.
                    # type: string
                # nstDesigner:
                    # description: NST Designer.
                    # type: string
                # nstInvariantId:
                    # description: >
                      # NST Identifier of a NST (is the same for different versions of a NST).
                    # type: string
                # nstNsdIds:
                  # description: List of underlying NSDs that comprise the NST.
                  # type: string
                # nstOnboardingState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
                # nstOperationalState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
                # nstUsageState:
                    # description: Usage state (IN_USE, NOT_IN_USE).
                    # type: string
                    # enum:
                      # - IN_USE
                      # - NOT_IN_USE
                # notificationTypes:
                    # description: >
                      # Permited values: NstOnboardingNotification, NStTChangeNotification and NstDeletionNotification.
                    # type: string
                    # enum:
                        # - NstOnboardingNotification
                        # - NstChangeNotification
                        # - NstDeletionNotification
                # userDefinedData: #this type is not the most accurate (hash?)
                  # description: NST user defined data.
                  # type: string
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI  
    # put:
      # description: |
        # **Upload NST**: Upload a NST after NST Info creation.
      # parameters:
        # - name: nst
          # description: Data associated to a NST.
          # in: body
          # required: true
          # schema:
            # description: NST.
            # type: object
            # required:
              # - id
            # properties:
              # id:
                  # description: >
                      # A unique identifier of the NST allocated by the Slice Manager.
                  # type: string
              # nstId:
                  # description: A unique identifier assigned by the NST designer.
                  # type: string
              # nstName:
                    # description: NST Name.
                    # type: string
              # nstVersion:
                    # description: NST Version.
                    # type: string
              # nstDesigner:
                    # description: NST Designer.
                    # type: string
              # nstInvariantId:
                    # description: >
                      # NST Identifier of a NST (is the same for different versions of a NST).
                    # type: string
              # nstNsdIds:
                  # description: List of underlying NSDs that comprise the NST.
                  # type: string
              # nstOnboardingState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
              # nstOperationalState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
              # nstUsageState:
                    # description: Usage state (IN_USE, NOT_IN_USE).
                    # type: string
                    # enum:
                      # - IN_USE
                      # - NOT_IN_USE
              # notificationTypes:
                    # description: >
                      # Permited values: NstOnboardingNotification, NStTChangeNotification and NstDeletionNotification.
                    # type: string
                    # enum:
                        # - NstOnboardingNotification
                        # - NstChangeNotification
                        # - NstDeletionNotification
              # userDefinedData: #this type is not the most accurate (hash?)
                  # description: NST user defined data.
                  # type: string
      # responses:
        # '201':
          # description: >
            # Unique identifier of the NST.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NSI instance.
              # type: string
              # format: url
          # schema:
            # description: >
              # A unique identifier of the NST Info.
              # type: integer
              # name: nstInfoId
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: > 
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI                
  # /subscriptions:
    # post:
      # description: |
        # **Subscribe**: Subscribes to notifications related to NSTs management changes.
      # parameters:
        # - name: filter
          # description: Filter to match the NSTs.
          # in: body
          # required: true
          # schema:
            # description: Filter to match the NSTs.
            # type: object
            # required:
              # - callbackUri
            # properties:
              # filter:
                # description: >
                  # A subscription filter related to notifications related to NST management.
                # type: object
                # properties:
                 # id:
                  # description: >
                      # A unique identifier of the NST allocated by the Slice Manager.
                  # type: string
                 # nstId:
                  # description: A unique identifier assigned by the NST designer.
                  # type: string
                 # nstName:
                    # description: NST Name.
                    # type: string
                 # nstVersion:
                    # description: NST Version.
                    # type: string
                 # nstDesigner:
                    # description: NST Designer.
                    # type: string
                 # nstInvariantId:
                    # description: >
                      # NST Identifier of a NST (is the same for different versions of a NST).
                    # type: string
                 # nstNsdIds:
                  # description: List of underlying NSDs that comprise the NST.
                  # type: string
                 # nstOnboardingState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
                 # nstOperationalState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
                 # nstUsageState:
                    # description: Usage state (IN_USE, NOT_IN_USE).
                    # type: string
                    # enum:
                      # - IN_USE
                      # - NOT_IN_USE
                 # notificationTypes:
                    # description: >
                      # Permited values: NstOnboardingNotification, NStTChangeNotification and NstDeletionNotification.
                    # type: string
                    # enum:
                        # - NstOnboardingNotification
                        # - NstChangeNotification
                        # - NstDeletionNotification
                 # userDefinedData: #this type is not the most accurate (hash?)
                  # description: NST user defined data.
                  # type: string
              # callbackUri:
                # description: |
                  # String formatted according to IETF RFC 3986.
                # type: string
              # authentication:
                # type: object
                # required:
                  # - authType
                # properties:
                  # authType:
                    # description: >
                      # Defines the type of Authentication Authorization to use.
                      # when sending a notification.  Permitted values: * BASIC:
                      # In every POST request that sends a notification, use
                        # HTTP Basic authentication with the client credentials.
                      # * OAUTH2_CLIENT_CREDENTIALS: In every POST request that
                      # sends a
                        # notification, use an OAuth 2.0 Bearer token, obtained using the
                        # client credentials grant type.
                    # type: string
                    # enum:
                      # - BASIC
                      # - OAUTH2_CLIENT_CREDENTIALS
                  # paramsBasic:
                    # description: >
                      # Parameters for authentication/authorization using BASIC.
                      # Shall be present if authType is "BASIC" and the contained
                      # information has not been provisioned out of band. Shall be
                      # absent otherwise.
                    # type: object
                    # properties:
                      # userName:
                        # description: >
                          # Username to be used in HTTP Basic authentication.
                          # Shall be present if it has not been provisioned out of
                          # band.
                        # type: string
                      # password:
                        # description: >
                          # Password to be used in HTTP Basic authentication.
                          # Shall be present if it has not been provisioned out of
                          # band.
                        # type: string
                  # paramsOauth2ClientCredentials:
                    # description: >
                      # Parameters for authentication/authorization using
                      # OAUTH2_CLIENT_CREDENTIALS. Shall be present if authType is
                      # "OAUTH2_CLIENT_CREDENTIALS" and the contained information
                      # has not been provisioned out of band. Shall be absent
                      # otherwise.
                    # type: object
                    # properties:
                      # clientId:
                        # description: >
                          # Client identifier to be used in the access token
                          # request of the OAuth 2.0 client credentials grant
                          # type.  Shall be present if it has not been provisioned
                          # out of band. The clientId and clientPassword passed in
                          # a subscription shall not be the same as the clientId
                          # and clientPassword that are used to obtain
                          # authorization for API requests. Client credentials may
                          # differ between subscriptions. The value of
                          # clientPassword should be generated by a random
                          # process.
                        # type: string
                      # clientPassword:
                        # description: >
                          # Client password to be used in the access token request
                          # of the OAuth 2.0 client credentials grant type.  Shall
                          # be present if it has not been provisioned out of band.
                          # The clientId and clientPassword passed in a
                          # subscription shall not be the same as the clientId and
                          # clientPassword that are used to obtain authorization
                          # for API requests. Client credentials may differ
                          # between subscriptions. The value of clientPassword
                          # should be generated by a random process.
                        # type: string
                      # tokenEndpoint:
                        # description: |
                          # String formatted according to IETF RFC 3986.
                        # type: string
      # responses:
        # '201':
          # description: >
            # Representation of the created subscription resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NST instance
              # type: string
              # format: url
          # schema:
            # type: array
            # items:
              # description: >
                # This type represents a subscription related to notifications about NST management.
              # type: object
              # required:
                # - id
                # - callbackUri
                # - _links
              # properties:
                # id:
                  # description: |
                    # Unique identifier of the subscription.
                  # type: string
                # callbackUri:
                  # description: |
                    # String formatted according to IETF RFC 3986.
                  # type: string
                # _links:
                  # description: |
                    # Links to resources related to this resource.
                  # type: object
                  # required:
                    # - self
                  # properties:
                    # self:
                      # description: |
                        # This type represents a link to a resource.
                      # type: object
                      # required:
                        # - href
                      # properties:
                        # href:
                          # description: |
                            # URI of the referenced resource.
                          # type: string
                          # format: url
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
    # get:
      # description: >
        # **Query Subscription Info**: Queries the subscription information.
      # responses:
        # '200':
          # description: >
            # Representation of the subscription resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
            # Location:
              # description: The resource URI of the created NST instance.
              # type: string
              # format: url
          # schema:
            # type: array
            # items:
              # description: >
                # This type represents a subscription related to notifications  about NST management.
              # type: object
              # required:
                # - id
                # - callbackUri
                # - _links
              # properties:
                # id:
                  # description: |
                    # Unique identifier of the subscription.
                  # type: string
                # callbackUri:
                  # description: |
                    # String formatted according to IETF RFC 3986.
                  # type: string
                # _links:
                  # description: |
                    # Links to resources related to this resource.
                  # type: object
                  # required:
                    # - self
                  # properties:
                    # self:
                      # description: |
                        # This type represents a link to a resource.
                      # type: object
                      # required:
                        # - href
                      # properties:
                        # href:
                          # description: |
                            # URI of the referenced resource.
                          # type: string
                          # format: url
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
  # '/subscriptions/{subscriptionId}':
    # parameters:
      # - name: subscriptionId
        # description: >
          # Unique identifier of this subscription.
        # in: path
        # type: string
        # required: true
    # get:
      # description: |
        # **Query Subscription Info**: Queries the subscription info related to NST management changes.
      # responses:
        # '200':
          # description: |
            # Representation of the subscription resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # This type represents a subscription related to notifications about NST management.
            # type: object
            # required:
              # - id
              # - callbackUri
              # - _links
            # properties:
              # id:
                # description: |
                  # A unique identifier of the subscription.
                # type: string
              # filter:
                # description: >
                  # This represents a subscription filter related to
                  # notifications related to NST management. 
                # type: object
                # properties:
                 # id:
                  # description: >
                      # A unique identifier of the NST allocated by the Slice Manager.
                  # type: string
                 # nstId:
                  # description: A unique identifier assigned by the NST designer.
                  # type: string
                 # nstName:
                    # description: NST Name.
                    # type: string
                 # nstVersion:
                    # description: NST Version.
                    # type: string
                 # nstDesigner:
                    # description: NST Designer.
                    # type: string
                 # nstInvariantId:
                    # description: >
                      # NST Identifier of a NST (is the same for different versions of a NST).
                    # type: string
                 # nstNsdIds:
                  # description: List of underlying NSDs that comprise the NST.
                  # type: string
                 # nstOnboardingState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
                 # nstOperationalState:
                    # description: Operational state (ENABLE, DISABLED).
                    # type: string
                    # enum:
                      # - ENABLED
                      # - DISABLED
                 # nstUsageState:
                    # description: Usage state (IN_USE, NOT_IN_USE).
                    # type: string
                    # enum:
                      # - IN_USE
                      # - NOT_IN_USE
                 # notificationTypes:
                    # description: >
                      # Permited values: NstOnboardingNotification, NStTChangeNotification and NstDeletionNotification.
                    # type: string
                    # enum:
                        # - NstOnboardingNotification
                        # - NstChangeNotification
                        # - NstDeletionNotification
                 # userDefinedData: #this type is not the most accurate (hash?)
                  # description: NST user defined data.
                  # type: string
              # callbackUri:
                # description: |
                  # String formatted according to IETF RFC 3986.
                # type: string
              # _links:
                # description: |
                  # Links to resources related to this resource.
                # type: object
                # required:
                  # - self
                # properties:
                  # self:
                    # description: |
                      # This type represents a link to a resource.
                    # type: object
                    # required:
                      # - href
                    # properties:
                      # href:
                        # description: |
                          # URI of the referenced resource.
                        # type: string
                        # format: url
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
    # delete:
      # description: |
        # Terminate Subscription: Terminate an existing subscription.
      # responses:
        # '204':
          # description: |
            # The subscription resource was deleted successfully.
          # headers:
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
        # '400':
          # description: >
            # If the request is malformed or syntactically incorrect.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '401':
          # description: >
            # If the request contains no access token even though one is required, or if the request contains an authorization token that is invalid.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '403':
          # description: >
            # If the API consumer is not allowed to perform a particular request to a particular resource, the API producer shall respond with this response code.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '404':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '405':
          # description: >
            # If the API producer did not find a current representation for the resource addressed by the URI passed in the request, or is not willing to disclose that one exists.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '406':
          # description: >
            # If the "Accept" HTTP header does not contain at least one name of a content type that is acceptable to the API producer.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '409':
          # description: >
            # Another request is in progress that prohibits the fulfillment of the current request, or the current resource state is inconsistent with the request.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '412':
          # description: >
            # This code is used in conjunction with conditional requests (typically used to protect resources consistency when using PUT or PATCH in a multi-client scenario) to indicate that a precondition has failed.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '416':
          # description: >
            # This code is returned if the requested byte range in the Range HTTP header is not present in the requested resource.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '422':
          # description: >
            # If the payload body of a request contains syntactically correct data
            # (e.g. well-formed JSON) but the data cannot be processed (e.g.
            # because it fails validation against a schema).
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response.
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # The definition of the general "ProblemDetails" data structure from
              # IETF RFC 7807 [19] is reproduced inthis structure. Compared to the
              # general framework defined in IETF RFC 7807 [19], the "status" and
              # "detail" attributes are mandated to be included by the present
              # document, to ensure that the response contains additional textual
              # information about an error. IETF RFC 7807 [19] foresees
              # extensibility of the "ProblemDetails" type. It is possible that
              # particular APIs in the present document, or particular
              # implementations, define extensions to define additional attributes
              # that provide more information about the error. The description
              # column only provides some explanation of the meaning to Facilitate
              # understanding of the design. For a full description, see IETF RFC
              # 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # A URI reference according to IETF RFC 3986 [5] that identifies
                  # the problem type. It is encouraged that the URI provides
                  # human-readable documentation for the problem (e.g. using HTML)
                  # when dereferenced. When this member is not present, its value
                  # is assumed to be "about:blank".
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type. It should
                  # not change from occurrence to occurrence of the problem,
                  # except for purposes of localization. If type is given and
                  # other than "about:blank", this attribute shall also be
                  # provided. A short, human-readable summary of the problem
                  # type.  It SHOULD NOT change from occurrence to occurrence of
                  # the problem, except for purposes of localization (e.g., using
                  # proactive content negotiation; see [RFC7231], Section 3.4).
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem. The
                  # HTTP status code ([RFC7231], Section 6) generated by the
                  # origin server for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A human-readable explanation specific to this occurrence of
                  # the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem. It may yield further information if dereferenced.
                # type: string
                # format: URI
        # '500':
          # description: >
            # If there is an application error not related to the client's input that cannot be easily mapped to any other HTTP response code ("catch all error").
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI
        # '503':
          # description: >
            # If the API producer encounters an internal overload situation of itself or of a system it relies on.
          # headers:
            # Content-Type:
              # description: The MIME type of the body of the response ("application/problem+json").
              # type: string
              # maximum: 1
              # minimum: 1
            # WWW-Authenticate:
              # description: >
                # Challenge if the corresponding HTTP request has not provided
                # authorization, or error details if the corresponding HTTP
                # request has provided an invalid authorization token.
              # type: string
              # maximum: 1
              # minimum: 0
          # schema:
            # description: >
              # General "ProblemDetails" data structure from IETF RFC 7807 [19].
            # type: object
            # required:
              # - status
              # - detail
            # properties:
              # type:
                # description: >
                  # Problem type according to IETF RFC 3986 [5]
                # type: string
                # format: URI
              # title:
                # description: >
                  # A short, human-readable summary of the problem type.
                # type: string
              # status:
                # description: >
                  # The HTTP status code for this occurrence of the problem.
                # type: integer
              # detail:
                # description: >
                  # A longer explanation of the specific occurrence of the problem.
                # type: string
              # instance:
                # description: >
                  # A URI reference that identifies the specific occurrence of the
                  # problem.
                # type: string
                # format: URI